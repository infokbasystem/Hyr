namespace Backend.Utils.Fortnox
{
    /// <summary>
    /// Fortnox OAuth app registration used both to build the authorize URL
    /// (parkoppling) and to exchange the resulting code for tokens.
    /// </summary>
    public static class FortnoxAppCredentials
    {
        public const string ClientId = "lbiXtlx8rx0I";
        public const string ClientSecret = "fX3OelQ883";
    }
}
