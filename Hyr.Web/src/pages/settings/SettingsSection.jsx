const SettingsSection = ({ title }) => {
    return (
        <div className="w-full">
            <h1 className="mt-20 text-center text-xl">{title}</h1>
        </div>
    );
};

export default SettingsSection;
